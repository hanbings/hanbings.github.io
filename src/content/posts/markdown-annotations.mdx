---
title: 'Markdown 边注、术语、聊天框、钢琴窗与五线谱测试'
description: 'MDX 富文本组件的使用说明与渲染测试，包括手绘标注、文章边注、术语原文、聊天记录、交互式钢琴窗和五线谱。'
date: '2026-07-18 14:00:00'
tags: ['markdown', '排版']
author: '寒冰'
---

export const contentFormat = 'MDX'

这篇文章既是本站 Markdown 扩展的使用说明，也是文章边注、术语原文、聊天记录、钢琴窗与五线谱的渲染测试，当前由 **{contentFormat}** 渲染。

## MDX 组件

本站将复杂的富文本能力注册为全局 MDX 组件。文章可以直接使用手绘标注、术语、边注、聊天框、钢琴窗和五线谱组件，无需在每篇文章中重复导入。普通 Markdown 继续使用 Astro 默认渲染器，适合不需要组件的内容。

## MDX 手绘标注

Rough Notation 的七种效果分别封装为 `<Underline>`、`<Box>`、`<Circle>`、`<Highlight>`、`<StrikeThrough>`、`<CrossedOff>` 和 `<Bracket>`：

```mdx
<Underline>下划线</Underline>
<Box>方框</Box>
<Circle>圆圈</Circle>
<Highlight>高亮</Highlight>
<StrikeThrough>删除线</StrikeThrough>
<CrossedOff>交叉划除</CrossedOff>
这句话中，<Bracket brackets={['left', 'right']}>只有这一小部分</Bracket> 会被标注。

段落前文 <Bracket class="inline-block w-52 align-middle" brackets={['left', 'right']}>这段较长的内容会自然换行，并由同一组括号包围</Bracket> 段落后文。

<Bracket class="block" brackets={['left', 'right']}>
  这是一整个被括号包裹的段落。它可以自然换行，并始终只绘制一组左右括号。
</Bracket>
```

<div className="grid gap-6 py-5">
  <div>
    <Underline>这是手绘下划线</Underline>
  </div>
  <div>
    <Box color="#78a9ff">这是方框</Box>
  </div>
  <div>
    <Circle color="#e88c9a">这是圆圈</Circle>
  </div>
  <div>
    <Highlight color="#ffe08a">这是高亮</Highlight>
  </div>
  <div>
    <StrikeThrough color="#9c6ade">这是删除线</StrikeThrough>
  </div>
  <div>
    <CrossedOff color="#e06c75">这是交叉划除</CrossedOff>
  </div>
  <div>
    {'这是一整句话，其中 '}
    <Bracket color="#ffe08a" brackets={['left', 'right']}>
      {'只有这一小部分'}
    </Bracket>
    {' 会被标注。'}
  </div>
  <div>
    {'段落前文 '}
    <Bracket class="inline-block w-52 align-middle" color="#ffe08a" brackets={['left', 'right']}>
      {'这段较长的内容会自然换行，并由同一组括号包围'}
    </Bracket>
    {' 段落后文。'}
  </div>
  <div>
    <Bracket class="mt-3 block" color="#ffe08a" brackets={['left', 'right']}>
      {'这是一整个被括号包裹的段落。它使用可用的正文宽度自然换行，并始终只绘制一组左右括号。'}
    </Bracket>
  </div>
</div>

所有组件都支持 `color`、`strokeWidth`、`iterations`、`padding`、`animationDuration`、`animate`、`multiline`、`rtl` 和 `class`。`<Bracket>` 额外支持 `brackets` 和 `gap`：`brackets` 可选择 `left`、`right`、`top` 和 `bottom`；`gap` 默认是 `0.75em`，用于为句内括号预留左右空间，可传入像素数字、任意 CSS 长度，或用 `gap={0}` 关闭。它默认将多行内容视为一个整体，避免为每一行重复绘制括号。无 JavaScript 时会保留对应的 CSS 降级效果；减少动态效果模式下会自动关闭绘制动画。

## 术语原文

MDX 页面可以使用全局注册的 `<Term>` 组件，通过 `original` 保留术语原文，并用 `lang` 标记原文语言：

```mdx
<Term original="virtual memory" lang="en">
  虚拟内存
</Term>
<Term original="上下文切换" lang="zh-CN">
  context switch
</Term>
```

例如，操作系统中的 <Term original="virtual memory" lang="en">虚拟内存</Term> 并不等同于物理内存；Rust 的 <Term original="borrow checker" lang="en">借用检查器</Term> 则会在编译期检查引用是否有效。

标注也可以反过来使用。例如在英文段落中，<Term original="上下文切换" lang="zh-CN">context switch</Term> 上方显示的是中文原词。`lang` 可以省略，但在中英文混排时建议保留。

`<Term>` 生成标准 HTML `<ruby>` 和 `<rt>` 元素。`<rt>` 可用于发音、翻译、音译或短注释。若内容需要离开本站的 Markdown pipeline，也可以直接使用标准 HTML，前提是目标渲染器允许 Markdown 中的 raw HTML：

```html
<ruby>虚拟内存<rt lang="en">virtual memory</rt></ruby>
```

## 文章附记

`<Sidenote>` 的内容就是卡片正文，紧跟其后的段落用 `<SidenoteText>` 标记对应文字。不再需要额外的卡片组、ID 或配对名称：

```mdx
<Sidenote>这里是附记，也支持普通的 **Markdown**。</Sidenote>

<p>
  <SidenoteText>这里是与附记相关的正文。</SidenoteText>
</p>
```

<Sidenote>
  这是一条附记。宽屏浏览时，它位于文章右侧；可用空间不足时，它会回到正文中，因而不会挤压主要内容。
</Sidenote>

<p>
  <SidenoteText>
    这段正文用来观察附记的位置和颜色关系。鼠标停在便签或这段文字上时，文字背景会显示相同颜色。边注适合补充背景、出处或不影响主线的细节；如果内容是理解文章所必需的，就不应该藏在边注里。
  </SidenoteText>
</p>

### 自定义标题与 Markdown

`title` 可以修改卡片标题；组件内容支持强调、代码、链接和多个段落：

<Sidenote title="背景">
  附记可以包含 **强调**、`inline code` 和[链接](https://www.markdownguide.org/)，也可以写成多个段落。

不过，它仍然应该尽量简短。

</Sidenote>

<p>
  <SidenoteText>这段正文用于检查自定义标题、富文本和多段内容。</SidenoteText>
</p>

### 嵌套为卡片堆

在一条 `<Sidenote>` 中继续嵌套 `<Sidenote>`，就会按出现顺序组成卡片堆。对应的 `<SidenoteText>` 使用相同顺序嵌套：第一张卡片对应外层文字，后面的卡片对应内层文字。

```mdx
<Sidenote title="整句">
  第一张附记对应整句话。

  <Sidenote title="局部">第二张附记只对应其中一部分。</Sidenote>
</Sidenote>

<p>
  <SidenoteText>
    这是整句话，其中<SidenoteText>这一部分</SidenoteText>另有一张附记。
  </SidenoteText>
</p>
```

<Sidenote title="背景">
  这是第一张卡片，用来补充整句话的背景信息。

  <Sidenote title="出处">这是第二张卡片，用来记录局部文字的资料出处。</Sidenote>
  <Sidenote title="延伸">这是第三张卡片，提供一个可以继续阅读的方向。</Sidenote>
</Sidenote>

<p>
  <SidenoteText>
    这句话同时关联了 <SidenoteText>背景和出处</SidenoteText>，也包含{' '}
    <SidenoteText>延伸阅读</SidenoteText>。
  </SidenoteText>
</p>

每张卡片保留自己的颜色和文字范围；点击当前卡片可以切换到下一张，把鼠标移到对应文字上也会切换到那张卡片。

## 钢琴窗

MDX 页面可以使用全局注册的 `<Piano>` 组件。它使用科学音高记号描述按下的琴键，例如中央 C 是 `C4`，升 C 可以写成 `C#4` 或 `C♯4`，降 D 也可以写成 `Db4`：

```mdx
<Piano title="C 大调和弦" notes="C4 E4 G4" />
```

<Piano title="C 大调和弦" notes="C4 E4 G4" />

`notes` 可以传入字符串或字符串数组，并使用空格、逗号或顿号分隔。浏览者可以点击琴键切换按下状态，钢琴窗会实时列出当前音符；“清空”按钮会释放全部琴键。

钢琴窗默认从 `C4` 开始显示两个八度。可以用 `octave` 指定起始八度，用 `octaves` 指定一到四个八度：

```mdx
<Piano title="低音区的 F 小调和弦" notes="F2 Ab2 C3" octave={2} octaves={2} />
```

<Piano title="低音区的 F 小调和弦" notes="F2 Ab2 C3" octave={2} octaves={2} />

降号会被规范到对应的升号琴键，因此上例中的 `Ab2` 显示为 `G♯2`。如果音名无法识别，或音符不在当前琴窗的音域内，构建会直接报错。省略 `notes` 则会生成一架没有预先按下琴键的钢琴：

```mdx
<Piano title="试着按下几个音" />
```

<Piano title="试着按下几个音" />

## 五线谱

`<MusicScore>` 使用 ABC 记谱文本生成响应式五线谱。例如，下面是一个八度的 C 大调音阶：

```mdx
<MusicScore
  title="C 大调音阶"
  abc={`X:1
M:4/4
L:1/4
K:C
C D E F | G A B c |]`}
/>
```

<MusicScore
  title="C 大调音阶"
  abc={`X:1
M:4/4
L:1/4
K:C
C D E F | G A B c |]`}
/>

`M:` 表示拍号，`L:` 表示默认音符时值，`K:` 表示调号。大写音名位于较低的八度，小写音名位于较高的八度；升号写作 `^`，降号写作 `_`。乐谱默认随文章宽度缩放，也可以用 `responsive={false}` 关闭，或用 `staffWidth={640}` 指定谱面宽度。JavaScript 不可用或内容进入 RSS 时，组件会保留 ABC 源码。

## 聊天记录

`<ChatBox>` 用来包住一段可以折叠的聊天记录，每一条消息使用 `<ChatMessage>`。`role="humen"` 的消息显示在右侧，`role="ai"` 的消息显示在左侧：

```mdx
<ChatBox title="关于 MDX 组件的讨论">
  <ChatAI slot="ai" name="OpenAI Codex" />

  <ChatMessage role="humen" time="10:24">
    可以在消息里使用 **Markdown** 吗？
  </ChatMessage>
  <ChatMessage role="ai" time="10:24">
    可以，其他 MDX 组件也可以正常嵌套。
  </ChatMessage>
</ChatBox>
```

<ChatBox title="关于 MDX 组件的讨论">
  <ChatAI slot="ai" name="OpenAI Codex" />

<ChatMessage role="humen" time="10:24">
  可以在消息里使用 **Markdown** 和边注吗？
</ChatMessage>

<Sidenote title="聊天记录的边注">边注位于消息气泡之外；聊天窗口折叠时，它也会一起隐藏。</Sidenote>

  <ChatMessage role="ai" time="10:24">
    可以。比如下面这些内容都可以正常显示：

    - 多个段落与列表
    - `inline code`
    - [文章中的链接](https://docs.astro.build/en/guides/integrations-guide/mdx/)

    <p>
      <SidenoteText>这段 AI 消息正文与外面的边注相互关联。</SidenoteText>
    </p>

  </ChatMessage>
  <ChatMessage role="humen" time="10:25">
    很好，这样我就能直接展示完整的对话了。
  </ChatMessage>
</ChatBox>

聊天窗口默认展开，点击标题栏即可折叠；传入 `open={false}` 可以让它初始保持折叠。`<ChatAI slot="ai" name="…">` 用来声明参与对话的 AI；省略时显示通用的 AI 提示。标题下方会明确说明 AI 内容未经验证，文章正文才是人类内容。聊天内容中的标题不会混入文章目录。消息气泡使用一套独立于文章正文的紧凑 Markdown 排版。

边注应作为 `<ChatBox>` 的直接子元素，紧跟在它所关联的 `<ChatMessage>` 之前；聊天框里的每条边注都会自动标记为“人类编写或已验证的信息”。宽屏时边注会浮到聊天窗口外侧，窄屏时则留在窗口内、消息气泡之外。折叠时消息和边注都会隐藏，只保留标题栏。两位对话人只通过左右位置和气泡颜色区分，不显示头像或昵称；`time` 和对应的 `datetime` 都是可选项。

## 窄屏、RSS 与无障碍

`<Sidenote>` 生成 `<aside role="note">`；嵌套卡片与嵌套正文按出现顺序配对。在手机或较窄的窗口中，单条附记会成为带有标题的正文块，多条附记仍可逐张切换；术语原文则继续显示在术语上方。聊天框会缩小内边距，并保持两位对话人的左右关系。

钢琴窗中的每个琴键都是带有按下状态的按钮，可以通过键盘聚焦并操作。琴键较多或屏幕较窄时，键盘区域可以横向滚动；RSS 中则保留标题与初始音符文字，不依赖交互脚本。

RSS 使用同一套内容组件，并保留每一张卡片对应的 `<aside>`、`<ruby>` 与 `<rt>` 语义；所有附记内容都会依次出现，因此不会依赖客户端交互脚本。

## 相关规范与文档

- [CommonMark](https://spec.commonmark.org/)
- [Astro Markdown](https://docs.astro.build/en/guides/markdown-content/)
- [Astro MDX](https://docs.astro.build/en/guides/integrations-guide/mdx/)
- [abcjs](https://docs.abcjs.net/)
- [Rough Notation](https://roughnotation.com/)
- [HTML `<rt>`](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/rt)
