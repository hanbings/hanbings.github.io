---
title: 'Astro、MDX 与 Shader'
description: '在博客内的 mdx 中支持 Shader 组件，并将 Astro 的 layout 分层，允许 Shader 覆盖全站'
date: '2026-07-20 03:45:12'
tags: ['markdown', 'astro', 'shader', 'glsl']
author: '寒冰'
---

下面的 `ShaderBox` 将 GLSL 保存在 MDX 文档内，在源码下方提供内联预览，并通过 `overlay` 开关提供“应用到页面”按钮。

<ShaderBox overlay>

```glsl
#version 300 es
precision highp float;

uniform vec2 u_resolution;
uniform float u_time;

out vec4 fragment_color;

void main() {
    vec2 uv = gl_FragCoord.xy / u_resolution;
    vec3 color = 0.5 + 0.5 * cos(u_time + uv.xyx * 6.28318 + vec3(0.0, 2.0, 4.0));

    fragment_color = vec4(color, 0.3);
}
```

</ShaderBox>
